// Game constants
const CELL_SIZE = 50;
const GRID_SIZE = 10;
const CANVAS_SIZE = CELL_SIZE * GRID_SIZE;

// Cell types
const EMPTY = 0;
const WALL = 1;
const PLAYER = 2;
const TARGET = 3;

// Directions
const UP = 0;
const RIGHT = 1;
const DOWN = 2;
const LEFT = 3;
